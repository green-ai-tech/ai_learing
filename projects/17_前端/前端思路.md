# 1. 前端的开发环境
```text
    vue(页面) + Axios(AJAX) + lucide(图标库) + Tai wind（样式表CSS）
       ｜
    vite7.3.3
       |                     ｜- 浏览器Javascript
    JavaSript + 第三方容器API -｜
                             ｜- 服务器javascipt -- node.exe(解释器)
                                                        |
                                                        |
                                                      npm.exe(Node Package Manager)
```
# 2. 创建项目工程

- Typescrpt是Javascipt的超集
- 其中所有选项中，我们只选择router其他的默认

# 3. 安装模块
    所有依赖

# 6.页面部署
目录说明：
    assets/ #资源
       样式表·图片·视频 ...
    
    components:
        组件（块：相对）
    views:
        工作区页面（一般都是配置路由：URL）
    router:
        为view.js自动挂载
1.  模仿：使用大模型生成页面
2.  导入：
        1. 组件
            - 头
            - 卡片
        2. 工作区
        3. 主页
